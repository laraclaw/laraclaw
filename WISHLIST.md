# Wishlist

- [ ] Refactor AppleCalendarDriver
- [ ] Review Console/Commands
- [ ] Review Jobs and following directories
- [ ] Implement or remove Terminal channel (ChannelType enum has the case, no class exists)
- [ ] Atomic Google OAuth token refresh (file_put_contents without lock — concurrent refreshes can corrupt the file)
- [ ] Enforce attachment size limits on email and Slack uploads before processing
- [ ] Add soft deletes to Reminder and Heartbeat models
- [ ] Make ChannelResolver extensible so consumers can register custom channels
